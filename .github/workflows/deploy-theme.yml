name: Deploy WordPress Theme

on:
  push:
    branches: [master]
    paths:
      - "wp-content/themes/**"
  workflow_dispatch: {} # ← lets you see/run it right away

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow detected ✅"
